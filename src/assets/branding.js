// Central place to manage app branding assets.
// If you add src/assets/logo.png, uncomment the import below and export it as logoSource.
// import logoPng from './logo.png';

/**
 * Exported logo source used by UI components. Keep null if no logo yet.
 * When you add logo.png, export it like:
 *   export const logoSource = logoPng;
 */
export const logoSource = null;

/**
 * Optional app display name for header/title fallback.
 */
export const appDisplayName = 'FoodWaste';

